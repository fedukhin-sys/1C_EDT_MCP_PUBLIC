# edt-mcp skill

Claude Code-скилл для работы с MCP-сервером **EDT_MCP** (плагин 1C:EDT, 84 инструмента).

## Что это

EDT_MCP — это OSGi-плагин для 1C:Enterprise Development Tools, который экспортирует HTTP+SSE MCP-сервер с 84 инструментами для:
- управления workspace/проектами;
- создания и редактирования метаданных 1С (Справочники, Документы, Регистры, Отчёты, СКД и т.д.);
- работы с BSL-модулями (read/write/list_methods/get_method);
- проверки качества (EDT-маркеры, SSL-чеки);
- управления инфобазами + деплой;
- запуска/остановки клиентов 1С (тонкий/толстый);
- отладки BSL (breakpoints, stack, evaluate);
- xUnitFor1C-тестов (create_test_module, run_tests).

Подробно: [GitHub `fedukhin-sys/EDT_MCP`](https://github.com/fedukhin-sys/EDT_MCP).

## Когда срабатывает

Используй этот скилл (или просто читай `SKILL.md`) когда нужно:
- создать/изменить объекты конфигурации 1С через MCP;
- сгенерировать форму, обработчики, СКД-отчёт;
- развернуть расширение на инфобазу;
- запустить тесты, отладку, клиента;
- разобраться с известными багами EDT_MCP и их обходами.

## Содержимое

- `SKILL.md` — полный reference: точные имена параметров всех 84 tools, рецепты типовых задач, список из 11 известных багов с workaround'ами, smoke-харнес, главные правила работы.

## Источник

Скилл собран на основе прохождения [final test-plan](https://github.com/fedukhin-sys/EDT_MCP/blob/main/docs/test-plan-with-tools.md) на проекте `ЕСС.РасширениеТЕСТ` в EDT 2026.1 с runtime 8.3.27. Версия MCP: v1.12.0 с последующим удалением 4 инструментов (`delete_project`, `import_project`, `delete_infobase`, `check_suppress_add`) — 84 tools.

Если в вашем проекте версия отличается — сверьтесь с `tools/list`, имена/наборы инструментов могли поменяться.
